apiVersion: v1
kind: Service
metadata:
  name: vpc-controller-webhook
  namespace: vpc-system
spec:
  selector:
    app.kubernetes.io/name: vpc-controller
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
